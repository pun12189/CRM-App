<UserControl x:Class="CallMan.Views.LoginLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallMan.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                xmlns:conv="clr-namespace:CallMan.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Padding="16" Margin="0,0,0,20">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="ShieldAccount" Width="24" Height="24" Foreground="#1794A1"/>
                    <TextBlock Text="Staff Login Audit Logs" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" DockPanel.Dock="Right">
                    <TextBox Width="250" materialDesign:HintAssist.Hint="Search Staff or Dept..." 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0"/>
                    <Button Command="{Binding LoadLogsCommand}" Style="{StaticResource MaterialDesignIconButton}">
                        <materialDesign:PackIcon Kind="Refresh"/>
                    </Button>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="1">
            <DataGrid ItemsSource="{Binding Logs}" AlternationCount="9999" GridLinesVisibility="All" BorderBrush="Black" BorderThickness="2" HorizontalGridLinesBrush="Black" VerticalGridLinesBrush="Black" AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Width="50">
                        <DataGridTextColumn.Binding>
                            <Binding Path="(ItemsControl.AlternationIndex)" 
     RelativeSource="{RelativeSource AncestorType=DataGridRow}">
                                <!-- We use a converter to start counting from 1 instead of 0 -->
                                <Binding.Converter>
                                    <conv:IndexToNumberConverter/>
                                </Binding.Converter>
                            </Binding>
                        </DataGridTextColumn.Binding>
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="Gray"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Timestamp" Binding="{Binding LoginTimestamp, StringFormat='dd-MMM-yy hh:mm tt'}" Width="*"/>
                    <DataGridTextColumn Header="Staff Member" Binding="{Binding StaffName}" FontWeight="Bold" Width="*"/>
                    <DataGridTextColumn Header="Department" Binding="{Binding DepartmentName}" Width="150"/>
                    <DataGridTextColumn Header="PC Name" Binding="{Binding MachineName}" Width="120"/>

                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="8,2" CornerRadius="10" Background="#E8F5E9">
                                    <TextBlock Text="{Binding Status}" Foreground="#2E7D32" HorizontalAlignment="Center" FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Logout Time" Binding="{Binding LogoutTimestamp, StringFormat='hh:mm tt'}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
