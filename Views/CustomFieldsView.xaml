<UserControl x:Class="CallMan.Views.CustomFieldsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallMan.Views"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="#F8FAFC">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="0,0,0,15" Padding="12,10" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left">
                    <TextBlock Text="Custom Fields" FontSize="18" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                    <TextBlock Text="Updation may take time up to 5 minutes to display results on the page" 
                               FontSize="12" FontWeight="SemiBold" Foreground="#EF4444" FontStyle="Italic" 
                               Margin="24,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" DockPanel.Dock="Right">
                    <Menu Background="#FF8A3D" Foreground="White" VerticalAlignment="Center" Margin="0,0,12,0">
                        <Menu.Style>
                            <Style TargetType="Menu">
                                <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="4"/>
                            </Style>
                        </Menu.Style>
                        <MenuItem Header="Action" FontWeight="SemiBold" FontSize="13" Height="32" Padding="15,5" VerticalAlignment="Center"/>
                        <MenuItem Height="32" Width="30" Padding="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                            <MenuItem.Header>
                                <materialDesign:PackIcon Kind="MenuDown" Width="20" Height="20" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </MenuItem.Header>
                            <MenuItem.Items>
                                <MenuItem Header="Batch Delete Selection" Height="36" FontSize="12" FontWeight="SemiBold" Background="White" Foreground="#334155"/>
                            </MenuItem.Items>
                        </MenuItem>
                    </Menu>

                    <Button Content="Add New" 
                            Command="{Binding OpenCreateFieldDialogCommand}"
                            Padding="16,0" Height="34" Background="#1794A1" BorderBrush="#1794A1" 
                            Foreground="White" FontWeight="Bold" Style="{StaticResource MaterialDesignFlatSecondaryDarkBgButton}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding CustomFieldsSource}" AutoGenerateColumns="False" 
          IsReadOnly="True" GridLinesVisibility="All" BorderBrush="#CBD5E1" BorderThickness="1" 
          HorizontalGridLinesBrush="#E2E8F0" VerticalGridLinesBrush="#E2E8F0" Background="White"
          RowHeight="46" HeadersVisibility="Column">

            <DataGrid.Resources>
                <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>

                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.Header>
                        <CheckBox HorizontalAlignment="Left" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                              HorizontalAlignment="Left" VerticalAlignment="Center" Margin="14,0,0,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="#" Binding="{Binding RowIndex}" Width="55">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Setter Property="Margin" Value="12,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Field Name" Binding="{Binding FieldName}" Width="2*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Medium"/>
                            <Setter Property="Foreground" Value="#1E293B"/>
                            <Setter Property="Margin" Value="10,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Type" Binding="{Binding FieldType}" Width="1.5*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="#475569"/>
                            <Setter Property="Margin" Value="10,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="Visible-In" Width="2.5*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="6,0">
                                <Border Background="#FFEAF2" CornerRadius="4" Padding="6,2" Margin="2"
                                Visibility="{Binding IsVisibleInLead, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Lead" FontSize="11" Foreground="#C01048" FontWeight="SemiBold"/>
                                </Border>
                                <Border Background="#E6F4EA" CornerRadius="4" Padding="6,2" Margin="2"
                                Visibility="{Binding IsVisibleInCustomer, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Customer" FontSize="11" Foreground="#137333" FontWeight="SemiBold"/>
                                </Border>
                                <Border Background="#E0F2FE" CornerRadius="4" Padding="6,2" Margin="2"
                                Visibility="{Binding IsVisibleInProduct, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Product" FontSize="11" Foreground="#0369A1" FontWeight="SemiBold"/>
                                </Border>
                            </WrapPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Value" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding SeedValueOptionsList}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StringFormat='{}{0}, '}" FontSize="12" Foreground="#475569" FontWeight="Medium"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Required" Width="1.2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0">
                                <TextBlock Text="No" Foreground="#94A3B8" FontWeight="Medium" x:Name="NotRequiredLabel"/>

                                <WrapPanel Orientation="Horizontal" x:Name="RequiredBadgesPanel" Visibility="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock Text="Yes (" Foreground="#DC2626" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                                    <TextBlock Text="Lead" Foreground="#DC2626" FontSize="11" FontWeight="Bold" ToolTip="Required in Leads" Visibility="{Binding IsRequiredInLead, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    <TextBlock Text="Customer" Foreground="#DC2626" FontSize="11" FontWeight="Bold" ToolTip="Required in Customers" Visibility="{Binding IsRequiredInCustomer, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="3,0,0,0"/>
                                    <TextBlock Text="Product" Foreground="#DC2626" FontSize="11" FontWeight="Bold" ToolTip="Required in Products" Visibility="{Binding IsRequiredInProduct, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="3,0,0,0"/>
                                    <TextBlock Text=")" Foreground="#DC2626" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                                </WrapPanel>
                            </Grid>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsRequired}" Value="True">
                                    <Setter TargetName="NotRequiredLabel" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="RequiredBadgesPanel" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Action" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0">
                                <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding DataContext.EditFieldCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="4" MinWidth="24" Height="24" Margin="0,0,6,0">
                                    <materialDesign:PackIcon Kind="Pencil" Foreground="#F59E0B" Width="16" Height="16"/>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding DataContext.DeleteFieldCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="4" MinWidth="24" Height="24">
                                    <materialDesign:PackIcon Kind="TrashCan" Foreground="#EF4444" Width="16" Height="16"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
