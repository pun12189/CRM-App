<UserControl x:Class="CallMan.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallMan.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Search & Add Bar -->
            <RowDefinition Height="Auto"/>
            <!-- Input Form (Collapsible) -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- 1. SEARCH & ACTION BAR -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox Width="300" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                 materialDesign:HintAssist.Hint="Search by Name or SKU" 
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            <Button Content="REFRESH" Command="{Binding RefreshCommand}" Margin="10,0" 
                Style="{StaticResource MaterialDesignFlatButton}"/>
        </StackPanel>

        <StackPanel Grid.Row="0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Content="IMPORT PRODUCTS" Command="{Binding OpenImportCommand}"  Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,15" Background="#1794A1" BorderBrush="#1794A1" Height="45" Padding="20,0"/>
            <Button Command="{Binding OpenAddProductCommand}"
            Style="{StaticResource MaterialDesignRaisedButton}"
            Background="#1794A1" 
            BorderBrush="#1794A1"
            Height="45" Padding="20,0" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="ADD NEW PRODUCT" VerticalAlignment="Center" FontWeight="Bold"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- 3. DATA LIST -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding FilteredProducts}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="Auto"/>
                <DataGridTextColumn Header="Product Name" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="*"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding InitialStock}" Width="Auto"/>
                <DataGridTextColumn Header="Selling Price" Binding="{Binding SellingPrice, StringFormat='₹{0}'}" Width="Auto"/>
                <DataGridTextColumn Header="Total Cost" Binding="{Binding TotalCost, StringFormat='₹{0}'}" Width="Auto"/>

                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                    <materialDesign:PackIcon Kind="Edit" Foreground="Orange"/>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                    <materialDesign:PackIcon Kind="Delete" Foreground="Red"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
