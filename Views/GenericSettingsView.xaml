<UserControl x:Class="CallMan.Views.GenericSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallMan.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title Bar -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- 1. TITLE BAR (Matches image) -->
        <DockPanel Grid.Row="0" Margin="10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBlock Text="{Binding PageTitle}" FontSize="20" FontWeight="SemiBold" Foreground="#444"/>
                <TextBlock Text="Updation may take time..." FontSize="12" Foreground="Red" Margin="20,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Export" Background="#FF8A3D" BorderBrush="#FF8A3D" Margin="0,0,10,0" Height="32"/>
                <Button Content="Add New" Command="{Binding OpenAddDialogCommand}" 
                    Background="#1794A1" BorderBrush="#1794A1" Height="32"/>
            </StackPanel>
        </DockPanel>

        <!-- 2. DATAGRID (Matches image) -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding ItemsList}" AutoGenerateColumns="False" 
              CanUserAddRows="False" SelectionMode="Single"
              Background="#F9F9F9" BorderThickness="1" BorderBrush="#DDD"
              RowHeight="50" ColumnHeaderHeight="40">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Width="60"/>
                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="40"/>

                <!-- Teal Name Column (Matches image) -->
                <DataGridTemplateColumn Header="Name" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" Foreground="#1794A1" VerticalAlignment="Center" Margin="5,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Total Leads" Binding="{Binding TotalLeads}" Width="*"/>

                <!-- Delete Leads Button (Matches image) -->
                <DataGridTemplateColumn Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Background="#E74C3C" BorderBrush="#E74C3C" Height="25" Width="100" FontSize="10"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Actions (Matches image) -->
                <DataGridTemplateColumn Header="Action" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource MaterialDesignIconButton}" Width="30" Height="30"
                                    Command="{Binding DataContext.EditItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                    <materialDesign:PackIcon Kind="Edit" Foreground="#FF8A3D" Width="16"/>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}" Width="30" Height="30"
                                    Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                    <materialDesign:PackIcon Kind="Delete" Foreground="#E74C3C" Width="16"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
