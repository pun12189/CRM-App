<UserControl x:Class="CallMan.Views.OrderHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallMan.Views"
             xmlns:converters="clr-namespace:CallMan.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F8FAFC">
    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
        <StackPanel>

            <!-- Header Section -->
            <DockPanel Margin="0,0,0,20">
                <TextBlock Text="Order Activity &amp; Audit Trail" FontSize="18" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding SelectedOrder.HistoryLogs.Count, StringFormat='({0} total events logged)'}" 
                           FontSize="13" Foreground="#64748B" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </DockPanel>

            <!-- Timeline Items List -->
            <ItemsControl ItemsSource="{Binding HistoryLogs}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Timeline Left Node & Vertical Connector Line -->
                            <Grid Grid.Column="0">
                                <Rectangle Width="2" Fill="#E2E8F0" HorizontalAlignment="Center" Margin="0,32,0,-16"/>
                                <Border CornerRadius="20" Width="36" Height="36" HorizontalAlignment="Center" VerticalAlignment="Top">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#64748B"/>
                                            <!-- Default Slate -->
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ActionType}" Value="OrderCreated">
                                                    <Setter Property="Background" Value="#0284C7"/>
                                                    <!-- Blue -->
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionType}" Value="PaymentAdded">
                                                    <Setter Property="Background" Value="#16A34A"/>
                                                    <!-- Green -->
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionType}" Value="StatusChange">
                                                    <Setter Property="Background" Value="#F59E0B"/>
                                                    <!-- Amber -->
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionType}" Value="Dispatched">
                                                    <Setter Property="Background" Value="#0D9488"/>
                                                    <!-- Teal -->
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <!-- Dynamic Action Icons -->
                                    <materialDesign:PackIcon Foreground="White" Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <materialDesign:PackIcon.Style>
                                            <Style TargetType="materialDesign:PackIcon">
                                                <Setter Property="Kind" Value="History"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="OrderCreated">
                                                        <Setter Property="Kind" Value="CartPlus"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="PaymentAdded">
                                                        <Setter Property="Kind" Value="CashMultiple"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="StatusChange">
                                                        <Setter Property="Kind" Value="ListStatus"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="Dispatched">
                                                        <Setter Property="Kind" Value="TruckDelivery"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="DocumentUploaded">
                                                        <Setter Property="Kind" Value="FileDocument"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </materialDesign:PackIcon.Style>
                                    </materialDesign:PackIcon>
                                </Border>
                            </Grid>

                            <!-- Timeline Card Content -->
                            <materialDesign:Card Grid.Column="1" Padding="16" Margin="8,0,0,0" Background="White" materialDesign:ElevationAssist.Elevation="Dp1">
                                <StackPanel>
                                    <!-- Title Bar -->
                                    <DockPanel Margin="0,0,0,6">
                                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                            <TextBlock Text="{Binding ActionTitle}" FontWeight="Bold" FontSize="14" Foreground="#1E293B"/>
                                            <Border Background="#F1F5F9" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding PerformedBy, StringFormat='By: {0}'}" FontSize="11" Foreground="#475569" FontWeight="SemiBold"/>
                                            </Border>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                                            <materialDesign:PackIcon Kind="ClockOutline" Width="14" Height="14" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <TextBlock Text="{Binding DisplayDate}" FontSize="12" Foreground="#64748B" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DockPanel>

                                    <Separator Background="#F1F5F9" Margin="0,4,0,8"/>

                                    <!-- Description / Note -->
                                    <TextBlock Text="{Binding Description}" Foreground="#334155" TextWrapping="Wrap" FontSize="13" LineHeight="18"/>

                                    <!-- Transaction Amount Badge (If Applicable) -->
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0" Visibility="{Binding TransactionAmount, Converter={StaticResource NullToVisibilityConverter}, FallbackValue=Collapsed}">
                                        <Border Background="#DCFCE7" CornerRadius="4" Padding="8,3">
                                            <TextBlock Text="{Binding TransactionAmount, StringFormat='Amount Processed: ₹ {0:N2}'}" Foreground="#15803D" FontWeight="Bold" FontSize="12"/>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </materialDesign:Card>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
