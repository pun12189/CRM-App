<Window x:Class="Tijori.Dialogs.FilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tijori.Dialogs"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Icon="/Assets/opn.png"
        xmlns:conv="clr-namespace:Tijori.Converters"
        Title="Filter" Width="600" Height="650"  ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent">
    <Window.Resources>
        <conv:PresetToColorConverter x:Key="PresetToColorConverter"/>
    </Window.Resources>
    <materialDesign:Card UniformCornerRadius="10" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="15">
                <TextBlock Text="Filter" FontSize="18" FontWeight="SemiBold" Foreground="#555"/>
                <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding CloseCommand}" HorizontalAlignment="Right" IsCancel="True">
                    <materialDesign:PackIcon Kind="Close"/>
                </Button>
            </DockPanel>

            <StackPanel Grid.Row="1" Margin="20,0">
                <TextBlock Text="Lead Holder" Foreground="#666" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding LeadHolders}" 
                          SelectedItem="{Binding SelectedLeadHolder}"
                          materialDesign:HintAssist.Hint="Select Leadholder"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          BorderBrush="#1794A1"/>
            </StackPanel>

            <Grid Grid.Row="2" Margin="20,20,20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <Button Content="Today" Command="{Binding SetPresetCommand}" CommandParameter="Today" Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left" Foreground="{Binding SelectedPreset, Converter={StaticResource PresetToColorConverter}, ConverterParameter=Today}"/>
                    <Button Content="Yesterday" Command="{Binding SetPresetCommand}" CommandParameter="Yesterday" Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left"/>
                    <Button Content="This Week" Command="{Binding SetPresetCommand}" CommandParameter="This Week" Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left"/>
                    <Button Content="Last Week" Command="{Binding SetPresetCommand}" CommandParameter="Last Week" Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left"/>
                    <Button Content="This Month" Command="{Binding SetPresetCommand}" CommandParameter="This Month" Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left"/>
                    <Button Content="Last Month" Command="{Binding SetPresetCommand}" CommandParameter="Last Month" Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left"/>

                    <Separator Margin="0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBox Text="1" Width="30" Margin="0,0,5,0"/>
                        <TextBlock Text="days up to today" VerticalAlignment="Center" FontSize="11"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border BorderBrush="#1794A1" BorderThickness="1" CornerRadius="4" Padding="15,5" Margin="5">
                            <TextBlock Text="{Binding StartDate, StringFormat='MMM dd, yyyy'}" Foreground="#1794A1"/>
                        </Border>
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="15,5" Margin="5">
                            <TextBlock Text="{Binding EndDate, StringFormat='MMM dd, yyyy'}" Foreground="#666"/>
                        </Border>
                    </StackPanel>

                    <Calendar SelectedDate="{Binding StartDate}" 
                              Style="{StaticResource MaterialDesignDatePickerCalendarPortrait}" 
                              HorizontalAlignment="Center" FontSize="12" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>

            <Button Grid.Row="3" Command="{Binding ApplyFilterCommand}"
                    Content="Filter" Background="#FF8A3D" BorderBrush="#FF8A3D"
                    Width="120" Height="35" Margin="0,20,0,20"/>
        </Grid>
    </materialDesign:Card>
</Window>
