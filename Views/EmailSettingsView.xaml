<UserControl x:Class="CallMan.Views.EmailSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallMan.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Padding="16" Margin="0,0,0,20">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="ShieldAccount" Width="24" Height="24" Foreground="#1794A1"/>
                    <TextBlock Text="Email Settings" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="10,0,0,0"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>
        <materialDesign:Card Grid.Row="1" Padding="30">
            <StackPanel>
                <TextBox Text="{Binding Settings.EmailAddress, UpdateSourceTrigger=PropertyChanged}" 
             materialDesign:HintAssist.Hint="Email Address" 
             Margin="0,0,0,16"/>

                <PasswordBox x:Name="SmtpPassword" 
                 materialDesign:HintAssist.Hint="SMTP Password / App Password" 
                 Margin="0,0,0,16"/>

                <Expander Header="Advanced Server Settings" Margin="0,0,0,16">
                    <StackPanel Margin="10">
                        <TextBox Text="{Binding Settings.Username}" 
                     materialDesign:HintAssist.Hint="SMTP Username (Usually same as email)" 
                     Margin="0,0,0,16"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding Settings.SmtpServer}" 
                         materialDesign:HintAssist.Hint="SMTP Server" Margin="0,0,8,16"/>
                            <TextBox Grid.Column="1" Text="{Binding Settings.Port}" 
                         materialDesign:HintAssist.Hint="Port" Margin="0,0,0,16"/>
                        </Grid>

                        <CheckBox IsChecked="{Binding Settings.EnableSSL}" Content="Enable SSL/TLS"/>
                    </StackPanel>
                </Expander>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="TEST CONNECTION" 
                        Style="{StaticResource MaterialDesignOutlinedButton}" 
                        Command="{Binding TestConnectionCommand}"
                        Margin="0,0,10,0"/>
                    <Button Content="SAVE SETTINGS" 
                        Background="#1794A1" 
                        Command="{Binding SaveSettingsCommand}"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
