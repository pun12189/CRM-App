<Window x:Class="Tijori.Dialogs.ImportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tijori.Dialogs"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Icon="/Assets/opn.png" ShowInTaskbar="False"
        Title="ImportView" Height="600" Width="800" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Padding="10" Margin="0,0,0,15">
            <DockPanel>
                <Button Content="BROWSE" Command="{Binding BrowseAndLoadCommand}" DockPanel.Dock="Right"/>
                <TextBox Text="{Binding FilePath}" IsReadOnly="True" materialDesign:HintAssist.Hint="Select Marg/Busy/Tally Export..."/>
            </DockPanel>
        </materialDesign:Card>

        <GroupBox Header="Step 1: Column Mapping" 
          Grid.Row="1" MinHeight="100"
          Margin="0,0,0,10"
          Visibility="{Binding IsMappingVisible, Converter={StaticResource BooleanToVisibilityConverter}}">

            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="300">
                <ItemsControl ItemsSource="{Binding Mappings}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="SemiBold"/>

                                <ComboBox Grid.Column="1" 
                                  ItemsSource="{Binding DataContext.ExcelHeaders, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                  SelectedItem="{Binding SelectedExcelHeader}"
                                  materialDesign:HintAssist.Hint="Select Excel Column"
                                  Margin="10,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <GroupBox Header="Step 2: Data Preview (Mapped Columns Only)" Grid.Row="2">
            <DataGrid ItemsSource="{Binding PreviewData}" 
              IsReadOnly="True" 
              AutoGenerateColumns="True"
              CanUserAddRows="False"
              VerticalScrollBarVisibility="Auto"
              MaxHeight="150"
              Background="WhiteSmoke">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                        <Setter Property="Background" Value="#F5F5F5"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
            </DataGrid>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="START IMPORT" Command="{Binding StartImportCommand}" Background="#1794A1"/>
        </StackPanel>
    </Grid>
</Window>
