<Window x:Class="CallMan.Dialogs.CustomerSummaryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CallMan.Dialogs"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="CustomerSummaryView" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header Name -->
            <RowDefinition Height="Auto"/>
            <!-- Details Card -->
            <RowDefinition Height="*"/>
            <!-- Tabs & Data -->
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Customer.CustomerName, StringFormat='{}{0} Summary'}" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- INFO CARD -->
        <materialDesign:Card Grid.Row="1" Padding="20" Margin="0,0,0,20" Background="#F5F5F5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <!-- Column 1: Contact -->
                <StackPanel Grid.Column="0">
                    <TextBlock><Run Text="Firm name: " FontWeight="Bold"/><Run Text="{Binding Customer.FirmName}"/></TextBlock>
                    <TextBlock Margin="0,5"><Run Text="Email: " FontWeight="Bold"/><Run Text="{Binding Customer.Email}" Foreground="#2196F3"/></TextBlock>
                    <TextBlock><Run Text="Phone: " FontWeight="Bold"/><Run Text="{Binding Customer.Phone}"/></TextBlock>
                    <TextBlock Margin="0,5"><Run Text="Location: " FontWeight="Bold"/><Run Text="{Binding Customer.State}"/></TextBlock>
                </StackPanel>

                <!-- Column 2: Status -->
                <StackPanel Grid.Column="1">
                    <TextBlock><Run Text="Customer Since: " FontWeight="Bold"/><Run Text="{Binding Metrics.CustomerSince}"/></TextBlock>
                    <TextBlock Margin="0,5"><Run Text="Last 3 months billing: " FontWeight="Bold"/><Run Text="{Binding Metrics.Last3MonthsBilling}"/></TextBlock>
                    <TextBlock><Run Text="Last order date: " FontWeight="Bold"/><Run Text="{Binding Metrics.LastOrderDate}"/></TextBlock>
                    <TextBlock Margin="0,5"><Run Text="Outstanding: " FontWeight="Bold"/><Run Text="{Binding Metrics.OutstandingAmount}" Foreground="Red"/></TextBlock>
                </StackPanel>

                <!-- Column 3: Business/Target -->
                <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Business Done/Target" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock><Run Text="Monthly: " Foreground="Red"/><Run Text="{Binding Metrics.MonthlyBusiness}"/></TextBlock>
                        <TextBlock><Run Text="Last 3 Months: " Foreground="Red"/><Run Text="{Binding Metrics.Last3MonthsBusiness}"/></TextBlock>
                        <TextBlock><Run Text="Overall: " Foreground="#2196F3"/><Run Text="{Binding Metrics.OverallBusiness}"/></TextBlock>
                    </StackPanel>
                </Border>
            </Grid>
        </materialDesign:Card>

        <!-- TABBED CONTENT -->
        <TabControl Grid.Row="2" Style="{StaticResource MaterialDesignTabControl}">
            <TabItem Header="Products Ordered">
                <DataGrid ItemsSource="{Binding ProductDetails}" AutoGenerateColumns="False" Margin="0,10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding Index}" Width="40"/>
                        <DataGridTextColumn Header="Total Orders" Binding="{Binding TotalOrders}" Width="*"/>
                        <DataGridTextColumn Header="Last Order" Binding="{Binding LastOrderDate}" Width="*"/>
                        <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="2*"/>
                        <DataGridTextColumn Header="Stock Left" Binding="{Binding Stock}" Width="*"/>
                        <DataGridTextColumn Header="Last Rate" Binding="{Binding LastRate}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Products Not Ordered">
                <!-- Similar DataGrid for potential products -->
            </TabItem>
        </TabControl>
    </Grid>
</Window>
