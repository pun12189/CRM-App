<UserControl x:Class="CallMan.Views.DepartmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallMan.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:CallMan.Converters"
             xmlns:security="clr-namespace:CallMan.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <materialDesign:BooleanToVisibilityConverter x:Key="InvertedVisibilityConverter" TrueValue="Collapsed" FalseValue="Visible" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: DEPARTMENT LIST -->
        <materialDesign:Card Grid.Column="0" Margin="0,0,24,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Margin="16" HorizontalAlignment="Stretch">
                    <TextBlock Text="Business Departments" Style="{StaticResource MaterialDesignHeadline6TextBlock}" HorizontalAlignment="Left"/>
                    <Button Command="{Binding AddNewDepartmentCommand}" security:SecurityGuard.Requires="Departments:Create" Content="ADD DEPARTMENT" HorizontalAlignment="Right"
                        Style="{StaticResource MaterialDesignRaisedButton}" Background="#1794A1"/>
                </DockPanel>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Departments, IsAsync=True}" SelectedItem="{Binding SelectedDepartment}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Sequence" Binding="{Binding SequenceOrder}" Width="*"/>
                        <DataGridTextColumn Header="Dept Name" Binding="{Binding DeptName}" Width="*"/>
                        <DataGridTextColumn Header="Head/Manager" Binding="{Binding DeptHead}" Width="*"/>
                        <DataGridTemplateColumn Header="Skip Repeat" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <materialDesign:PackIcon Kind="FastForward" 
                                     Foreground="#FF9800"
                                     HorizontalAlignment="Center"
                                     Visibility="{Binding SkipOnRepeat, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Status" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <materialDesign:PackIcon Kind="CheckDecagram" Foreground="#4CAF50" 
                                                     HorizontalAlignment="Center"
                                                     Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Action" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                    Command="{Binding DataContext.DeleteDepartmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}" security:SecurityGuard.Requires="Departments:Delete">
                                        <materialDesign:PackIcon Kind="DeleteOutline" Foreground="Red"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- RIGHT: EDITOR PANEL -->
        <materialDesign:Card Grid.Column="1" Padding="20" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Department Setup" FontWeight="Bold" FontSize="16" Margin="0,0,0,20"/>

                <TextBox Text="{Binding SelectedDepartment.DeptName}" materialDesign:HintAssist.Hint="Department Name" Margin="0,0,0,16"/>
                <TextBox Text="{Binding SelectedDepartment.SequenceOrder}" materialDesign:HintAssist.Hint="Workflow Sequence (1, 2, 3...)" Margin="0,0,0,16"/>

                <!-- Workflow Rules Section -->
                <Border Background="#E1F5FE" Padding="12" CornerRadius="4" Margin="0,0,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <materialDesign:PackIcon Kind="AxisArrow" Foreground="#0288D1" VerticalAlignment="Center"/>
                            <TextBlock Text="Workflow Logic" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <CheckBox IsChecked="{Binding SelectedDepartment.SkipOnRepeat}" 
                          Content="Skip this department for repeat orders" 
                          ToolTip="If the customer has a previous order, this step is bypassed."/>
                    </StackPanel>
                </Border>

                <CheckBox IsChecked="{Binding SelectedDepartment.IsActive}" Content="Active" Margin="0,0,0,20"/>

                <Button Command="{Binding SaveSelectedDepartmentCommand}" Content="SAVE DEPARTMENT" security:SecurityGuard.Requires="Departments:Edit"
                Height="45" Background="#1794A1"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
