<UserControl x:Class="CallMan.Views.MaturedLeadsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallMan.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Background="#B71C1C" Foreground="White" Padding="15" Margin="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Total Market Outstanding: " VerticalAlignment="Center"/>
                <TextBlock Text="{Binding TotalOutstanding, StringFormat='₹ {0:N2}'}" FontSize="20" FontWeight="Bold" Margin="10,0,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <DataGrid Grid.Row="1" ItemsSource="{Binding MaturedLeads}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="Total Bills" Binding="{Binding TotalOrderAmount, StringFormat='₹ {0:N0}'}" Width="100"/>
                <DataGridTextColumn Header="Paid" Binding="{Binding TotalPaidAmount, StringFormat='₹ {0:N0}'}" Width="100"/>
                <DataGridTextColumn Header="Balance" Binding="{Binding TotalBalanceDue, StringFormat='₹ {0:N0}'}" Foreground="Red" FontWeight="Bold" Width="100"/>
                <DataGridTemplateColumn Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Manage Orders" Command="{Binding DataContext.OpenOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
