<UserControl x:Class="Tijori.Views.WorkflowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tijori.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="#F8FAFC">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Action Header -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Workflows" FontSize="18" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="+ Add New" 
                        Command="{Binding OpenCreateDialogCommand}"
                        Background="#0D9488" BorderBrush="Transparent" Foreground="White"
                        FontWeight="SemiBold" FontSize="11" Height="28" Padding="12,0"/>
            </StackPanel>
        </Grid>

        <!-- Workflows High-Density DataGrid -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="4">
            <DataGrid ItemsSource="{Binding Workflows}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      HorizontalGridLinesBrush="#E2E8F0" RowHeight="32" Background="White">

                <!-- Crisp Compact Column Header -->
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F1F5F9"/>
                        <Setter Property="Foreground" Value="#475569"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#CBD5E1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <!-- Checkbox Column -->
                    <DataGridTemplateColumn Width="36">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Row ID -->
                    <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="45">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Foreground" Value="#64748B"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Title -->
                    <DataGridTextColumn Header="Title" Binding="{Binding WorkflowName}" Width="2.5*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#1E293B"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="4,0,0,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Event Name -->
                    <DataGridTextColumn Header="Event" Binding="{Binding EventName}" Width="2*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Foreground" Value="#334155"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Channels Indicator -->
                    <DataGridTemplateColumn Header="Channels" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Background="#DCFCE7" CornerRadius="2" Padding="4,1" Margin="0,0,4,0"
                                            Visibility="{Binding SendWhatsApp, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="WA" FontSize="9" FontWeight="Bold" Foreground="#16A34A"/>
                                    </Border>
                                    <Border Background="#E0F2FE" CornerRadius="2" Padding="4,1" Margin="0,0,4,0"
                                            Visibility="{Binding SendEmail, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="Email" FontSize="9" FontWeight="Bold" Foreground="#0284C7"/>
                                    </Border>
                                    <Border Background="#FEF3C7" CornerRadius="2" Padding="4,1"
                                            Visibility="{Binding SendNotification, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="Toast" FontSize="9" FontWeight="Bold" Foreground="#D97706"/>
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Created At -->
                    <DataGridTextColumn Header="Created at" Binding="{Binding CreatedAt, StringFormat='MM-dd-yyyy hh:mm tt'}" Width="1.5*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="10"/>
                                <Setter Property="Foreground" Value="#64748B"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Row Action Buttons (Pencil & Trash) -->
                    <DataGridTemplateColumn Header="Action" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <!-- Edit -->
                                    <Button Command="{Binding DataContext.EditWorkflowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignIconButton}" Width="24" Height="24" Padding="0">
                                        <materialDesign:PackIcon Kind="Pencil" Foreground="#F59E0B" Width="14" Height="14"/>
                                    </Button>
                                    <!-- Delete -->
                                    <Button Command="{Binding DataContext.DeleteWorkflowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignIconButton}" Width="24" Height="24" Padding="0" Margin="4,0,0,0">
                                        <materialDesign:PackIcon Kind="Delete" Foreground="#EF4444" Width="14" Height="14"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
