<UserControl x:Class="Tijori.Views.ImportDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tijori.Views"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="400" Padding="20" Background="White">
    <StackPanel>
        <TextBlock Text="Import / Upload Documents" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>

        <!-- Category Dropdown -->
        <ComboBox materialDesign:HintAssist.Hint="Target Category Slot"
                  ItemsSource="{Binding AvailableDocumentCategories}"
                  DisplayMemberPath="CategoryName"
                  SelectedItem="{Binding SelectedUploadCategory}"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,16"/>

        <!-- Multi-File Picker Button -->
        <Button Command="{Binding BrowseFilesCommand}"
                Style="{StaticResource MaterialDesignOutlinedButton}" Height="40" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="FolderOpen" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="Browse File(s)" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- Selected Files List -->
        <TextBlock Text="{Binding SelectedFilesSummaryText}" Foreground="#64748B" FontSize="12" Margin="0,0,0,16"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" 
                    Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,8,0"/>
            <Button Content="Upload Now" Command="{Binding UploadSelectedFilesCommand}" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Background="#059669"/>
        </StackPanel>
    </StackPanel>
</UserControl>
